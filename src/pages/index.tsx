import React from 'react';
import Layout from '@/components/Layout';

const Home = (): JSX.Element => (
  <main>
    <Layout>
      <h1>Home Page</h1>
    </Layout>
  </main>
);

export default Home;
